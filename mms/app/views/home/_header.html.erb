<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to 'MMS App', root_path, class:"navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        
        <li class="nav-item">
            <%= link_to 'About', home_about_path, class:"nav-link"%>
        </li>
        <li class="nav-item">
            <%= link_to 'View Materials', materials_path, class:"nav-link"%>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
              <%= link_to 'Add Material', new_material_path, class:"nav-link"%>
          </li>
        <% end %>
        <%# user auth actions %>
        <% if !user_signed_in? %>
          <li class="nav-item">
              <%= link_to 'Sign up', new_user_registration_path, class:"nav-link"%>
          </li>
          <li class="nav-item">
              <%= link_to 'Sign In', new_user_session_path, class:"nav-link"%>
          </li>
        <% else %>
          <li class="nav-item">
              <%= button_to 'Sign out', destroy_user_session_path, class:"nav-link", method: :delete%>
          </li>
          <li class="nav-item">
              <%= link_to 'Edit Profile', edit_user_registration_path, class:"nav-link"%>
          </li>
        <% end %>   
      </ul>
      <%= form_with url: search_materials_path, method: :get, remote: true, html: { class: "d-flex", role: "search" } do |form| %>
        <%= form.text_field :query, placeholder: "Search Materials", value: params[:query], class: "form-control me-2" %>
        <%= form.submit "Search", class: "btn btn-outline-success" %>
      <% end %>

    </div>
  </div>
</nav>